import { ArtDetailed } from "@/features/Arts/ArtDetailed";

export default function Art() {
  return (
    <div className="mx-[20vw] mb-[80px] mt-[80px]">
      <ArtDetailed></ArtDetailed>
    </div>
  );
}
